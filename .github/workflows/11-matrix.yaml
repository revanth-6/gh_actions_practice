name: 11-Matrix

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: [16,18,20]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Printing matrix
        run: echo "Node ${{ matrix.node }}"